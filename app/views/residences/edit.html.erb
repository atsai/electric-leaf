<h1>Editing residence</h1>

<% form_for(@residence) do |f| %>
  <%= f.error_messages %>

  <p>
    <%= f.submit 'Update' %>
  </p>
<% end %>

<%= link_to 'Show', @residence %> |
<%= link_to 'Back', residences_path %>